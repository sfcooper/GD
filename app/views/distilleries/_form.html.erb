<%= javascript_pack_tag 'countryselector' %>
<%= form_with(model: distillery, local: true) do |form| %>
  <% if distillery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(distillery.errors.count, "error") %> prohibited this distillery from being saved:</h2>

      <ul>
      <% distillery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: "input-label" %>
    <%= form.text_field :name, class: "input" %>
  </div>

  <div class="field">
    <%= form.label :snippet, class: "input-label" %>
    <%= form.text_area :snippet, class: "input"  %>
  </div>

  <div class="field">
    <%= form.label :description, class: "input-label" %>
    <%= form.text_area :description, class: "input"  %>
  </div>

  <div id="country-selector"></div>

  <br />
    <div class="field">
    <%= form.label :website, class: "input-label" %>
    <%= form.url_field :website, class: "input", placeholder: "https://..."  %>
  </div>

  <div class="actions">
    <%= form.submit class: "button" %>
  </div>
<% end %>
